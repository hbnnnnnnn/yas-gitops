
apiVersion: argoproj.io/v1alpha1

kind: Application

metadata:

  name: staging-inventory

  namespace: argocd

spec:

  project: default

  sources:

    - repoURL: https://github.com/your-username/yas

      targetRevision: main

      path: k8s/charts/inventory

      helm:

        valueFiles:

          - $values/staging/inventory.values.yaml

    - repoURL: https://github.com/your-org/yas-gitops

      targetRevision: HEAD

      ref: values

  destination:

    server: https://kubernetes.default.svc

    namespace: staging

  syncPolicy:

    automated:

      prune: true

      selfHeal: true

